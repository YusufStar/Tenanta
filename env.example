# Tenanta Environment Variables (Root Level)
# This file contains only general and Docker Compose environment variables
# Each application has its own env.example file in its directory

# =============================================================================
# Docker Compose Configuration
# =============================================================================

# PostgreSQL Database (for Docker Compose)
POSTGRES_DB=tenanta
POSTGRES_USER=tenanta_user
POSTGRES_PASSWORD=tenanta_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis (for Docker Compose)
REDIS_PASSWORD=tenanta_redis_password
REDIS_HOST=redis
REDIS_PORT=6379

# =============================================================================
# General Application Configuration
# =============================================================================

# Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# API Configuration
API_VERSION=v1
API_PREFIX=/api

# CORS
CORS_ORIGIN=http://localhost:3000

# =============================================================================
# Security Configuration
# =============================================================================

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# Schema Management (Global Settings)
# =============================================================================

# Schema Validation
SCHEMA_VALIDATION_ENABLED=true
SCHEMA_VERSIONING_ENABLED=true

# Schema Limits
MAX_SCHEMAS_PER_TENANT=50
MAX_TABLES_PER_SCHEMA=100
MAX_COLUMNS_PER_TABLE=100

# Schema Naming Rules
SCHEMA_NAME_PATTERN=^[a-z][a-z0-9_]*$
TABLE_NAME_PATTERN=^[a-z][a-z0-9_]*$
COLUMN_NAME_PATTERN=^[a-z][a-z0-9_]*$

# =============================================================================
# Development Configuration
# =============================================================================

# Debug Mode
DEBUG=tenanta:*

# Development Ports
DATABASE_API_PORT=3001
CLIENT_PORT=3000
WS_PORT=3002

# =============================================================================
# Application-specific Environment Files
# =============================================================================

# Each application has its own env.example file:
# - apps/database-api/env.example
# - apps/client/env.example
# - apps/database/env.example
#
# Copy the appropriate env.example to .env in each app directory
# and update the values according to your setup.